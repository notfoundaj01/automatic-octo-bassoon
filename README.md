# Log Integrity Checker 🔐

A lightweight log file integrity monitoring tool using SHA-256 hashing and HMAC verification.

## Features

- SHA-256 hashing
- Directory or single file support
- Tamper detection
- HMAC-signed baseline storage
- Manual re-initialization
- No external dependencies

## Installation

```bash
git clone https://github.com/YOUR_USERNAME/log-integrity-checker.git
cd log-integrity-checker
chmod +x integrity-check
